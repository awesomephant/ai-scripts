- `readJSONFile()`
  - renamed to `readJsonFile()`
  - extracted (unused)
- `readYamlConfigFile()`
  - extracted (unused)
- `convertAiColor()`
  - extracted
  - renamed to `aiColorToCss()`
  - refactored source
  - added unit tests
  - extracted `darkGrayToBlack()`
- `getAllArtboardBounds()`
  - extracted
  - added unit tests
  - refactored source
  - now returns `null` instead of `undefined` when passed an empty array
- dropped `getArtboardSettings()`, `parseObjectName()` does the same thing
- dropped `getDocumentSlug()`
- extracted `makeList()`
- extracted `extendFontlist()`
- extracted `formatError()`
- extracted `replaceSvgIds()`
- extracted `getArtboardUniqueName()`
- extracted `getCircleData()`
- extracted `getRectangleData()`
- extracted `getLineGeometry()`
- extracted `generateOutputHtml()`
- extracted `findLayers()`
- `getTextStyleClass()`
  - Used to return a class name if found or mutate the classes array in place if not, now it only does the former
  - Renamed to `getTextStyleClassName()`
